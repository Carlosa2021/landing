import { ReactNode } from 'react';

export const metadata = {
  title: 'Portafolio | ChainX',
  description: 'Casos reales de tokenización y Web3 desarrollados por ChainX.',
};

export default function PortfolioLayout({
  children,
}: {
  children: ReactNode;
}) {
  return children;
}
